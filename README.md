# Full Stack Application

This is a full-stack application with NestJS backend and Angular frontend.

## Project Structure

```
project/
├── backend/          # NestJS backend application
├── frontend/         # Angular frontend application (to be created)
└── README.md         # This file
```

## Backend (NestJS)

The backend is built with NestJS framework and TypeScript.

### Getting Started

```bash
cd backend
npm install
npm run start:dev
```

The backend will be available at `http://localhost:3000`

### Available Scripts

- `npm run start` - Start the application
- `npm run start:dev` - Start in development mode with hot reload
- `npm run build` - Build the application
- `npm run test` - Run tests
- `npm run test:e2e` - Run end-to-end tests
- `npm run lint` - Run ESLint

## Frontend (Angular)

Frontend will be created here once the application topic is decided.

## Development

This project is set up for TypeScript development with:
- NestJS for backend API
- Angular for frontend (to be added)
- Hot reload for development
- Testing setup included
- ESLint and Prettier for code formatting
